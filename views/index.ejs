<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Find Any Vinyl</title>
  <<style>
    body {
      background-color: #111;
      color: #fff;
      font-family: 'Arial', sans-serif;
      text-align: center;
      padding: 40px;
    }
  
    h1 {
      font-size: 3rem;
      color: orange;
    }
  
    form {
      margin-top: 30px;
    }
  
    input[type="text"] {
      padding: 10px;
      width: 300px;
      font-size: 16px;
      border-radius: 8px;
      border: none;
      outline: none;
    }
  
    button {
      padding: 10px 20px;
      font-size: 16px;
      margin-left: 10px;
      background-color: orange;
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }
  
    .results {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      margin-top: 40px;
      max-width: 1200px;
      margin-left: auto;
      margin-right: auto;
    }
  
    .card {
      width: 180px;
      background: #222;
      border-radius: 10px;
      padding: 10px;
      color: white;
      text-align: center;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
  
    .card:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 12px rgba(255, 165, 0, 0.3);
    }
  
    .card img {
      width: 100%;
      border-radius: 6px;
    }
  
    .card-title {
      margin: 10px 0 5px;
      font-weight: bold;
    }
  
    .card-label,
    .card-meta {
      font-size: 12px;
      color: #bbb;
    }
  
    @media (max-width: 500px) {
      .card {
        width: 100%;
      }
  
      input[type="text"] {
        width: 90%;
      }
    }
  </style>
  <script>
  window.addEventListener('load', () => {
    const results = document.querySelector('.results');
    if (results && results.children.length > 0) {
      results.scrollIntoView({ behavior: 'smooth' });
    }
  });
</script>

</head>
<script>
  window.addEventListener('load', () => {
    const results = document.querySelector('.results');
    if (results && results.children.length > 0) {
      results.scrollIntoView({ behavior: 'smooth' });
    }
  });
</script>

<body>
  <h1><span class="emoji">🎵</span>Find Any Vinyl</h1>

  <form action="/search" method="POST">
    <input type="text" name="query" placeholder="Search for Artist, Album, Vinyl..." />
    <button type="submit">Search</button>
  </form>  

  <div class="results">
    <% if (result && result.length > 0) { %>
      <% result.forEach(item => { %>
        <a href="https://www.discogs.com<%= item.url %>" target="_blank" style="text-decoration: none;">
          <div class="card">
            <% if (item.thumb) { %>
              <img src="<%= item.thumb %>" alt="Cover" />
            <% } else { %>
              <div style="width: 100%; height: 180px; background: #444; display: flex; align-items: center; justify-content: center; font-size: 14px;">No Image</div>
            <% } %>
            <p class="card-title"><%= item.title %></p>
            <p class="card-label"><%= item.label %></p>
            <p class="card-meta"><%= item.format %> | <%= item.year %></p>
  
            <a
              href="https://www.ebay.co.uk/sch/i.html?_nkw=<%= encodeURIComponent(item.title + ' vinyl') %>"
              target="_blank"
              style="display: inline-block; margin-top: 10px; padding: 8px 12px; font-size: 13px; background-color: #f0c14b; color: #111; border-radius: 6px; text-decoration: none;"
            >
              Search on eBay
            </a>
          </div>
        </a>
      <% }); %>
    <% } else if (result) { %>
      <p>No results found.</p>
    <% } %>
  </div>
  
    
</body>
</html>